describe('validating', function()
    it('should be passed', function()
        -- given:
        local cosmosline = require('compline.cosmosline')

        -- when:
        cosmosline:validate()
    end)
end)
